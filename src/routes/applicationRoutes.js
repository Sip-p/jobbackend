import express from 'express';
const router = express.Router();

router.post("/apply/:jobId",);
router.get("/:jobId/applicants",);
router.get("/my-applications",);
router.put("/update-status/:applicationId",);

export default router;